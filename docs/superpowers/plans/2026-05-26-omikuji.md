# おみくじインタラクション実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** トップページのプロフィール画像領域を、クリックで振って結果が出るおみくじインタラクションに置き換える。

**Architecture:** `Omikuji.tsx`（React Client Component）が状態機械 `initial → shaking → revealed → shaking → ...` を持ち、純粋ロジック（型・定数・抽選関数）は `omikuji.ts` に分離してテスト可能にする。アニメーションは Tailwind keyframes（`shake`、`fadeIn`）で実装、`prefers-reduced-motion` 環境ではシェイクを skip。

**Tech Stack:** Next.js 14 (App Router) / React 18 / TypeScript / Tailwind CSS 3.4 / vitest（node 環境） / Storybook 7.6

**Spec:** `docs/superpowers/specs/2026-05-26-omikuji-design.md`

---

## ファイル構成

| 操作 | パス | 責務 |
|---|---|---|
| 新規 | `src/components/omikuji.ts` | 型定義・カード定数・抽選関数（純粋ロジック） |
| 新規 | `src/components/omikuji.test.ts` | 抽選関数のユニットテスト |
| 新規 | `src/components/Omikuji.tsx` | React コンポーネント（状態機械・描画） |
| 新規 | `src/components/Omikuji.stories.tsx` | Storybook |
| 編集 | `tailwind.config.js` | `shake`・`fadeIn` keyframes と animation 追加 |
| 編集 | `src/app/page.tsx` | `<Avatar />` を `<Omikuji />` に差し替え |
| 削除 | `src/components/Avatar.tsx` | 用途消滅 |
| 削除 | `src/components/Avatar.stories.tsx` | 用途消滅 |
| 削除 | `public/images/profile-image.webp` | 用途消滅 |

---

## Task 1: Tailwind に shake / fadeIn アニメーションを追加

**Files:**
- Modify: `tailwind.config.js`

- [ ] **Step 1: 現状の `tailwind.config.js` を確認**

Run: `cat tailwind.config.js`

期待される現状（参考、変更不要部分）:

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {
    extend: {
      keyframes: {
        rotateCoin: {
          "0%": { transform: "rotateY(0deg)" },
          "100%": { transform: "rotateY(180deg)" },
        },
      },
      animation: {
        "rotate-coin": "rotateCoin 0.5s linear forwards",
      },
      ...
```

- [ ] **Step 2: `tailwind.config.js` を以下の内容に書き換える**

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {
    extend: {
      keyframes: {
        rotateCoin: {
          "0%": { transform: "rotateY(0deg)" },
          "100%": { transform: "rotateY(180deg)" },
        },
        shake: {
          "0%, 100%": { transform: "translateX(0) rotate(0deg)" },
          "15%": { transform: "translateX(-8px) rotate(-4deg)" },
          "30%": { transform: "translateX(8px) rotate(4deg)" },
          "45%": { transform: "translateX(-6px) rotate(-3deg)" },
          "60%": { transform: "translateX(6px) rotate(3deg)" },
          "75%": { transform: "translateX(-3px) rotate(-1deg)" },
        },
        fadeIn: {
          "0%": { opacity: "0" },
          "100%": { opacity: "1" },
        },
      },
      animation: {
        "rotate-coin": "rotateCoin 0.5s linear forwards",
        "shake-omikuji": "shake 0.6s ease-in-out",
        "fade-in": "fadeIn 0.2s ease-out",
      },
      backgroundImage: {
        "gradient-radial": "radial-gradient(var(--tw-gradient-stops))",
        "gradient-conic": "conic-gradient(from 180deg at 50% 50%, var(--tw-gradient-stops))",
      },
    },
  },
  plugins: [require("tailwindcss-react-aria-components")],
};
```

- [ ] **Step 3: Biome で format 確認**

Run: `pnpm format`
Expected: no errors (config ファイルは format 対象外なら no-op)

- [ ] **Step 4: Commit**

```bash
git add tailwind.config.js
git commit -m "feat(tailwind): add shake and fadeIn keyframes for omikuji"
```

---

## Task 2: おみくじ抽選ロジック（TDD）

**Files:**
- Create: `src/components/omikuji.ts`
- Create: `src/components/omikuji.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

Create `src/components/omikuji.test.ts`:

```typescript
import { describe, expect, it } from "vitest";
import { OMIKUJI_CARDS, pickRandomOmikuji } from "./omikuji";

describe("OMIKUJI_CARDS", () => {
  it("contains exactly 6 cards", () => {
    expect(OMIKUJI_CARDS).toHaveLength(6);
  });

  it("weights sum to 1.0", () => {
    const total = OMIKUJI_CARDS.reduce((sum, c) => sum + c.weight, 0);
    expect(total).toBeCloseTo(1.0, 10);
  });

  it("has unique keys", () => {
    const keys = OMIKUJI_CARDS.map((c) => c.key);
    expect(new Set(keys).size).toBe(6);
  });

  it("each card has a label, src, and positive weight", () => {
    for (const card of OMIKUJI_CARDS) {
      expect(card.label.length).toBeGreaterThan(0);
      expect(card.src).toMatch(/^\/assets\/omikuji\/.+\.png$/);
      expect(card.weight).toBeGreaterThan(0);
    }
  });
});

describe("pickRandomOmikuji", () => {
  it("returns daikichi for random 0.0", () => {
    expect(pickRandomOmikuji(() => 0.0).key).toBe("daikichi");
  });

  it("returns daikichi for random just under 0.3", () => {
    expect(pickRandomOmikuji(() => 0.29999).key).toBe("daikichi");
  });

  it("returns chu-kichi for random 0.3", () => {
    expect(pickRandomOmikuji(() => 0.3).key).toBe("chu-kichi");
  });

  it("returns sho-kichi for random 0.5", () => {
    expect(pickRandomOmikuji(() => 0.5).key).toBe("sho-kichi");
  });

  it("returns kichi for random 0.7", () => {
    expect(pickRandomOmikuji(() => 0.7).key).toBe("kichi");
  });

  it("returns kyo for random 0.85", () => {
    expect(pickRandomOmikuji(() => 0.85).key).toBe("kyo");
  });

  it("returns daikyo for random 0.95", () => {
    expect(pickRandomOmikuji(() => 0.95).key).toBe("daikyo");
  });

  it("returns daikyo for random just under 1.0 (floating point safety)", () => {
    expect(pickRandomOmikuji(() => 0.999999).key).toBe("daikyo");
  });

  it("uses Math.random by default and returns one of the defined cards", () => {
    const result = pickRandomOmikuji();
    expect(OMIKUJI_CARDS).toContainEqual(result);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm test src/components/omikuji.test.ts`
Expected: FAIL with "Cannot find module './omikuji'" もしくは類似のインポートエラー

- [ ] **Step 3: 最小限の実装を書く**

Create `src/components/omikuji.ts`:

```typescript
export type OmikujiResult =
  | "daikichi"
  | "chu-kichi"
  | "sho-kichi"
  | "kichi"
  | "kyo"
  | "daikyo";

export type OmikujiCard = {
  key: OmikujiResult;
  label: string;
  src: string;
  weight: number;
};

export const OMIKUJI_CARDS: ReadonlyArray<OmikujiCard> = [
  { key: "daikichi", label: "大吉", src: "/assets/omikuji/daikichi.png", weight: 0.3 },
  { key: "chu-kichi", label: "中吉", src: "/assets/omikuji/chu-kichi.png", weight: 0.2 },
  { key: "sho-kichi", label: "小吉", src: "/assets/omikuji/sho-kichi.png", weight: 0.2 },
  { key: "kichi", label: "吉", src: "/assets/omikuji/kichi.png", weight: 0.15 },
  { key: "kyo", label: "凶", src: "/assets/omikuji/kyo.png", weight: 0.1 },
  { key: "daikyo", label: "大凶", src: "/assets/omikuji/daikyo.png", weight: 0.05 },
];

export const pickRandomOmikuji = (random: () => number = Math.random): OmikujiCard => {
  const r = random();
  let cumulative = 0;
  for (const card of OMIKUJI_CARDS) {
    cumulative += card.weight;
    if (r < cumulative) return card;
  }
  return OMIKUJI_CARDS[OMIKUJI_CARDS.length - 1];
};
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm test src/components/omikuji.test.ts`
Expected: PASS (12 tests)

- [ ] **Step 5: Lint 確認**

Run: `pnpm lint`
Expected: 既存以上のエラー増加なし

- [ ] **Step 6: Commit**

```bash
git add src/components/omikuji.ts src/components/omikuji.test.ts
git commit -m "feat(omikuji): add weighted random picker and card definitions"
```

---

## Task 3: Omikuji コンポーネント本体

**Files:**
- Create: `src/components/Omikuji.tsx`

- [ ] **Step 1: コンポーネントを作成**

Create `src/components/Omikuji.tsx`:

```tsx
"use client";

import Image from "next/image";
import { useState } from "react";
import { type OmikujiCard, pickRandomOmikuji } from "./omikuji";

type State =
  | { phase: "initial" }
  | { phase: "shaking"; nextCard: OmikujiCard; previousCard: OmikujiCard | null }
  | { phase: "revealed"; card: OmikujiCard };

const ClosedCard = () => (
  <div
    className="
      h-40 w-40 md:h-72 md:w-72 rounded-full
      bg-[#f8efd9] border-4 border-red-700
      flex items-center justify-center shadow-md
    "
  >
    <span className="text-red-700 font-serif text-6xl md:text-9xl font-bold select-none">?</span>
  </div>
);

const prefersReducedMotion = () => {
  if (typeof window === "undefined") return false;
  return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
};

export const Omikuji = () => {
  const [state, setState] = useState<State>({ phase: "initial" });

  const handleClick = () => {
    if (state.phase === "shaking") return;
    const nextCard = pickRandomOmikuji();

    if (prefersReducedMotion()) {
      setState({ phase: "revealed", card: nextCard });
      return;
    }

    const previousCard = state.phase === "revealed" ? state.card : null;
    setState({ phase: "shaking", nextCard, previousCard });
  };

  const handleAnimationEnd = (e: React.AnimationEvent<HTMLButtonElement>) => {
    if (state.phase !== "shaking") return;
    if (e.animationName !== "shake") return;
    setState({ phase: "revealed", card: state.nextCard });
  };

  const isShaking = state.phase === "shaking";
  const visibleCard =
    state.phase === "initial"
      ? null
      : state.phase === "shaking"
        ? state.previousCard
        : state.card;

  const ariaLabel = state.phase === "initial" ? "おみくじを引く" : "もう一度おみくじを引く";

  return (
    <button
      type="button"
      aria-label={ariaLabel}
      onClick={handleClick}
      onAnimationEnd={handleAnimationEnd}
      className={`h-40 w-40 md:h-72 md:w-72 order-first md:order-last mb-10 ${
        isShaking ? "animate-shake-omikuji" : ""
      }`}
    >
      {visibleCard ? (
        <Image
          key={visibleCard.key}
          src={visibleCard.src}
          alt={visibleCard.label}
          width={1500}
          height={1500}
          className="animate-fade-in"
        />
      ) : (
        <ClosedCard />
      )}
    </button>
  );
};
```

- [ ] **Step 2: TypeScript 型エラーがないことを確認**

Run: `pnpm exec tsc --noEmit`
Expected: エラーなし（既存エラーは除く）

- [ ] **Step 3: Lint 確認**

Run: `pnpm lint`
Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add src/components/Omikuji.tsx
git commit -m "feat(omikuji): add Omikuji component with shake animation"
```

---

## Task 4: Omikuji Storybook

**Files:**
- Create: `src/components/Omikuji.stories.tsx`

- [ ] **Step 1: 参照として既存の `Avatar.stories.tsx` を確認**

Run: `cat src/components/Avatar.stories.tsx`

期待される内容（参考）:

```tsx
import type { StoryObj } from "@storybook/react";

import { Avatar } from "./Avatar";

const meta = {
  title: "Avatar",
  component: Avatar,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
```

- [ ] **Step 2: Storybook ファイルを作成**

Create `src/components/Omikuji.stories.tsx`:

```tsx
import type { StoryObj } from "@storybook/react";

import { Omikuji } from "./Omikuji";

const meta = {
  title: "Omikuji",
  component: Omikuji,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
```

- [ ] **Step 3: Lint 確認**

Run: `pnpm lint`
Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add src/components/Omikuji.stories.tsx
git commit -m "feat(omikuji): add Storybook story"
```

---

## Task 5: page.tsx を Omikuji に差し替え

**Files:**
- Modify: `src/app/page.tsx:3` (import) and `src/app/page.tsx:60` (usage)

- [ ] **Step 1: import 文を変更**

In `src/app/page.tsx`, find:

```tsx
import { Avatar } from "@/components/Avatar";
```

Replace with:

```tsx
import { Omikuji } from "@/components/Omikuji";
```

- [ ] **Step 2: 使用箇所を変更**

In `src/app/page.tsx`, find:

```tsx
          <Avatar />
```

Replace with:

```tsx
          <Omikuji />
```

- [ ] **Step 3: TypeScript と Lint を確認**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat(home): replace Avatar with Omikuji on hero"
```

---

## Task 6: 旧 Avatar 関連ファイルを削除

**Files:**
- Delete: `src/components/Avatar.tsx`
- Delete: `src/components/Avatar.stories.tsx`
- Delete: `public/images/profile-image.webp`

- [ ] **Step 1: 削除前に他参照がないことを確認**

Run: `grep -r "Avatar\|profile-image" src public --include="*.ts" --include="*.tsx" --include="*.js" --include="*.json" --include="*.md"`
Expected: 上記3ファイル以外への参照なし（あれば停止してユーザーに報告）

- [ ] **Step 2: ファイルを削除**

```bash
rm src/components/Avatar.tsx
rm src/components/Avatar.stories.tsx
rm public/images/profile-image.webp
```

- [ ] **Step 3: TypeScript・Lint・Test を確認**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test`
Expected: すべて成功

- [ ] **Step 4: Commit**

```bash
git add -A src/components/Avatar.tsx src/components/Avatar.stories.tsx public/images/profile-image.webp
git commit -m "chore: remove unused Avatar component and profile image"
```

注: 削除済みファイルを `git add` する場合、`git rm` を使うか `git add -A` で削除を追跡できる。

---

## Task 7: 全体動作確認

**Files:** なし（検証のみ）

- [ ] **Step 1: テストスイート全体を実行**

Run: `pnpm test`
Expected: 全テスト PASS

- [ ] **Step 2: Lint を実行**

Run: `pnpm lint`
Expected: エラーなし

- [ ] **Step 3: Production build が通ることを確認（型チェック含む）**

Run: `pnpm build`
Expected: ビルド成功、エラーなし

- [ ] **Step 4: 開発サーバーを起動して手動確認**

Run: `pnpm dev` （バックグラウンドまたは別ターミナル）

ブラウザで `http://localhost:3000` を開き、以下を確認:

1. プロフィール画像領域に「?」柄の丸カードが表示される
2. カードをクリックすると左右に振動するアニメーションが再生される
3. アニメーション終了後、6種類のいずれかのおみくじ結果画像にフェードイン切り替わる
4. もう一度クリックすると、現在の結果カードが振動 → 別（または同じ）の結果に切り替わる
5. ブラウザの DevTools で `prefers-reduced-motion: reduce` をエミュレートし、シェイクなしに即フェードで切り替わることを確認
6. キーボード操作（Tab → Enter / Space）でも同じ動作になることを確認
7. スマホ幅（375px等）でレイアウトが崩れないことを確認

- [ ] **Step 5: 何度かクリックして 6 種類すべての出現を確認（確率なので時間がかかる場合あり）**

特に大凶（5%）は稀。20–30回程度クリックして偏りが極端でないことを目視確認。

- [ ] **Step 6: 動作確認結果を報告**

問題なければ完了。問題があれば該当 Task に戻って修正。

---

## Self-Review

### Spec coverage

- 初期表示「?」カード → Task 3 (`ClosedCard`)
- クリックで振動 → Task 1 (keyframes) + Task 3 (state machine)
- 結果フェードイン → Task 1 (keyframes) + Task 3 (`animate-fade-in` + `key={visibleCard.key}`)
- 再クリック対応 → Task 3 (state machine の revealed → shaking 遷移)
- ラベルなし、画像のみ → Task 3 (テキスト出力なし)
- prefers-reduced-motion 対応 → Task 3 (`prefersReducedMotion()` 関数)
- aria-label → Task 3 (`ariaLabel` 変数)
- 抽選確率（30/20/20/15/10/5）→ Task 2 (`OMIKUJI_CARDS`)
- 6種類すべての画像参照 → Task 2 (`OMIKUJI_CARDS.src`)
- Avatar 等の削除 → Task 6
- page.tsx 差し替え → Task 5

すべての要件をカバー。

### Placeholder scan

「実装時に画像から微調整」は spec 側の文言。本 plan ではカラーコード `#f8efd9` と `red-700` を明示済み。

### Type consistency

- `OmikujiResult` / `OmikujiCard` / `pickRandomOmikuji` / `OMIKUJI_CARDS` は Task 2 と Task 3 で一貫
- State の `previousCard` プロパティ名は Task 3 内で一貫
- アニメーション名 `shake-omikuji` / `fade-in` は Task 1 と Task 3 で一貫
- keyframes 名 `shake` （`animationName` 一致確認用）は Task 1 と Task 3 で一貫
